# Basic Bash Commands Solutions

 
```bash
**Exercise 1**
ls -d ~/[A-Z]*

**Exercise 2**
ls -d ~/.*

**Exercise 3**
ls -d ~/.* | wc -l

**Exercise 4**
ls -d ~/[A-Za-z]*

**Exercise 5**
ls -d ~/[^A-Z]*

**Exercise 6**
ls -d ~/!(*.???) 2>/dev/null

**Exercise 7**
ls -d /etc/c*y

**Exercise 8**
ls -d /etc/*ss*

**Exercise 9**
ls -d ~/??[A-Z]?[A-Z]?e*

**Exercise 10**
ls -d ~/[A-Za-z0-9][A-Za-z0-9][A-Za-z0-9][A-Za-z0-9]

**Exercise 11**
ls -d /var/log/*.log
